import * as React from 'react';
import { HashRouter } from 'react-router-dom';
import './index.css';
import Root from 'pages/Root';

const App = () => (
    <HashRouter>
        <Root />
    </HashRouter>
)

export default App;
